import * as U from 'karet.util';
import * as L from 'partial.lenses';

export const scenesIn = U.view('scenes');

export const OBS = {
  scenesIn: U.view('scenes'),
  currentSceneIn: U.view('current-scene')
};
